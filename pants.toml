[GLOBAL]
pants_version = "2.13.0"
backend_packages = ["pants.backend.docker", "pants.backend.python"]

[python]
default_resolve = "default"
enable_resolves = true
interpreter_constraints = ["==3.9.*"]
lockfile_generator = "pex"

[python.resolves]
default = "3rdparty/python/default.lock"

[anonymous-telemetry]
enabled = true
# Randomly generated with `uuidgen --random`
repo_id = "30b442b0-4b33-4ebc-b50c-a3c7d627afba"
